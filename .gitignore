# Build output — regenerable, never commit
build/
*.o
*.a

# Stray binary if built outside build/
/bh_sim

# macOS noise
.DS_Store

# Editor swap files
*.swp
*~

# VS Code — ignore per-user settings, but share the build/debug configs
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json